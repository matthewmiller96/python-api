# Production environment - separate frontend and API
ENV=production
API_PORT=3000
FRONTEND_PORT=80
DOCKERFILE=Dockerfile
API_COMMAND=uvicorn app.main:app --host 0.0.0.0 --port 8000
RESTART_POLICY=unless-stopped
COMPOSE_PROFILES=full
